services:
  - type: web
    name: secure-chatbot-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.m4_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GROQ_API_KEY
        sync: false
